<!DOCTYPE html>
<html lang="zh-CN">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Jackie · 个人主页</title>
<link rel="preconnect" href="https://fonts.googleapis.com">
<link href="https://fonts.googleapis.com/css2?family=Noto+Serif+SC:wght@300;400;600;700;900&family=Playfair+Display:ital,wght@0,700;1,400&family=DM+Mono:wght@400;500&display=swap" rel="stylesheet">
<style>
  :root {
    --bg: #0E0F0D;
    --bg2: #151612;
    --bg3: #1C1D19;
    --gold: #C9A84C;
    --gold-light: #E2C47A;
    --gold-dim: rgba(201,168,76,0.15);
    --white: #F4F1EB;
    --white-dim: rgba(244,241,235,0.55);
    --white-faint: rgba(244,241,235,0.15);
    --green: #5C7A3E;
    --green-light: #7FA354;
    --border: rgba(244,241,235,0.08);
  }

  * { margin:0; padding:0; box-sizing:border-box; }
  html { scroll-behavior: smooth; }

  body {
    background: var(--bg);
    color: var(--white);
    font-family: 'Noto Serif SC', serif;
    overflow-x: hidden;
    cursor: none;
  }

  /* custom cursor */
  .cursor {
    position: fixed;
    width: 8px; height: 8px;
    background: var(--gold);
    border-radius: 50%;
    pointer-events: none;
    z-index: 9999;
    transform: translate(-50%, -50%);
    transition: transform 0.1s ease;
  }
  .cursor-ring {
    position: fixed;
    width: 32px; height: 32px;
    border: 1px solid rgba(201,168,76,0.4);
    border-radius: 50%;
    pointer-events: none;
    z-index: 9998;
    transform: translate(-50%, -50%);
    transition: all 0.18s ease;
  }

  /* NAV */
  nav {
    position: fixed; top:0; left:0; right:0; z-index:100;
    padding: 24px 7vw;
    display: flex;
    justify-content: space-between;
    align-items: center;
    background: rgba(14,15,13,0.85);
    backdrop-filter: blur(16px);
    border-bottom: 1px solid var(--border);
  }

  .nav-name {
    font-size: 15px;
    font-weight: 700;
    letter-spacing: 0.08em;
    color: var(--white);
  }

  .nav-name span { color: var(--gold); }

  .nav-links {
    display: flex; gap: 36px; list-style: none;
  }

  .nav-links a {
    font-size: 12px;
    letter-spacing: 0.12em;
    text-transform: uppercase;
    color: var(--white-dim);
    text-decoration: none;
    transition: color 0.2s;
  }

  .nav-links a:hover { color: var(--gold-light); }

  /* HERO */
  .hero {
    min-height: 100vh;
    display: flex;
    flex-direction: column;
    justify-content: flex-end;
    padding: 0 7vw 10vh;
    position: relative;
    overflow: hidden;
  }

  .hero-bg {
    position: absolute; inset: 0;
    background:
      radial-gradient(ellipse 60% 50% at 80% 20%, rgba(201,168,76,0.06) 0%, transparent 60%),
      radial-gradient(ellipse 40% 60% at 20% 80%, rgba(92,122,62,0.08) 0%, transparent 60%);
  }

  .hero-grid-overlay {
    position: absolute; inset: 0;
    background-image:
      linear-gradient(var(--border) 1px, transparent 1px),
      linear-gradient(90deg, var(--border) 1px, transparent 1px);
    background-size: 80px 80px;
    mask-image: radial-gradient(ellipse 70% 70% at 50% 50%, black 0%, transparent 100%);
    opacity: 0.4;
  }

  .hero-eyebrow {
    font-size: 11px;
    letter-spacing: 0.2em;
    text-transform: uppercase;
    color: var(--gold);
    margin-bottom: 24px;
    font-weight: 400;
    font-family: 'DM Mono', monospace;
    opacity: 0;
    animation: fadeUp 0.8s ease forwards 0.2s;
  }

  .hero-name {
    font-family: 'Playfair Display', 'Noto Serif SC', serif;
    font-size: clamp(56px, 8vw, 120px);
    line-height: 0.95;
    font-weight: 700;
    margin-bottom: 32px;
    opacity: 0;
    animation: fadeUp 0.8s ease forwards 0.4s;
  }

  .hero-name em {
    font-style: italic;
    color: var(--gold);
    font-weight: 400;
  }

  .hero-desc {
    font-size: clamp(16px, 1.8vw, 22px);
    color: var(--white-dim);
    line-height: 1.7;
    max-width: 600px;
    font-weight: 300;
    margin-bottom: 48px;
    opacity: 0;
    animation: fadeUp 0.8s ease forwards 0.6s;
  }

  .hero-desc strong { color: var(--white); font-weight: 600; }

  .hero-tags {
    display: flex; gap: 12px; flex-wrap: wrap;
    opacity: 0;
    animation: fadeUp 0.8s ease forwards 0.8s;
  }

  .hero-tag {
    border: 1px solid var(--border);
    border-radius: 100px;
    padding: 8px 20px;
    font-size: 12px;
    letter-spacing: 0.08em;
    color: var(--white-dim);
    font-family: 'DM Mono', monospace;
    transition: all 0.25s;
  }

  .hero-tag:hover {
    border-color: var(--gold);
    color: var(--gold-light);
    background: var(--gold-dim);
  }

  .hero-tag.active {
    border-color: var(--gold);
    color: var(--gold);
    background: var(--gold-dim);
  }

  .hero-scroll {
    position: absolute;
    bottom: 40px; right: 7vw;
    writing-mode: vertical-rl;
    font-size: 10px;
    letter-spacing: 0.2em;
    text-transform: uppercase;
    color: var(--white-faint);
    font-family: 'DM Mono', monospace;
    display: flex;
    align-items: center;
    gap: 12px;
    animation: fadeIn 1s ease forwards 1.2s;
    opacity: 0;
  }

  .hero-scroll::after {
    content: '';
    width: 1px;
    height: 60px;
    background: linear-gradient(to bottom, var(--gold), transparent);
    animation: scrollLine 2s ease-in-out infinite;
  }

  @keyframes scrollLine {
    0%, 100% { transform: scaleY(1); opacity: 0.4; }
    50% { transform: scaleY(0.6); opacity: 1; }
  }

  @keyframes fadeUp {
    from { opacity: 0; transform: translateY(24px); }
    to { opacity: 1; transform: translateY(0); }
  }

  @keyframes fadeIn {
    from { opacity: 0; }
    to { opacity: 1; }
  }

  /* SECTIONS */
  section { padding: 100px 7vw; }

  .section-label {
    font-family: 'DM Mono', monospace;
    font-size: 10px;
    letter-spacing: 0.25em;
    text-transform: uppercase;
    color: var(--gold);
    margin-bottom: 48px;
    display: flex;
    align-items: center;
    gap: 16px;
  }

  .section-label::after {
    content: '';
    flex: 1;
    max-width: 60px;
    height: 1px;
    background: var(--gold);
    opacity: 0.4;
  }

  /* NOW */
  .now { background: var(--bg2); border-top: 1px solid var(--border); }

  .now-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 2px;
  }

  .now-card {
    background: var(--bg3);
    padding: 36px 32px;
    transition: background 0.3s;
  }

  .now-card:hover { background: #222319; }

  .now-icon {
    font-size: 28px; margin-bottom: 16px;
  }

  .now-card-title {
    font-size: 13px;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: var(--gold);
    margin-bottom: 10px;
    font-family: 'DM Mono', monospace;
    font-weight: 500;
  }

  .now-card-body {
    font-size: 15px;
    line-height: 1.8;
    color: var(--white-dim);
    font-weight: 300;
  }

  .now-card-body strong { color: var(--white); font-weight: 600; }

  /* PROJECTS */
  .projects { background: var(--bg); }

  .project-item {
    display: grid;
    grid-template-columns: 1fr 2fr;
    gap: 80px;
    padding: 60px 0;
    border-bottom: 1px solid var(--border);
    align-items: start;
    transition: opacity 0.3s;
  }

  .project-item:first-of-type { border-top: 1px solid var(--border); }

  .project-meta {
    position: sticky;
    top: 100px;
  }

  .project-num {
    font-family: 'Playfair Display', serif;
    font-size: 80px;
    line-height: 1;
    color: var(--border);
    margin-bottom: 16px;
    font-style: italic;
  }

  .project-tag {
    display: inline-block;
    font-family: 'DM Mono', monospace;
    font-size: 10px;
    letter-spacing: 0.15em;
    text-transform: uppercase;
    padding: 5px 14px;
    border-radius: 100px;
    margin-bottom: 16px;
  }

  .project-tag.gold { background: var(--gold-dim); color: var(--gold); border: 1px solid rgba(201,168,76,0.2); }
  .project-tag.green { background: rgba(92,122,62,0.15); color: var(--green-light); border: 1px solid rgba(92,122,62,0.2); }

  .project-period {
    font-family: 'DM Mono', monospace;
    font-size: 11px;
    color: var(--white-faint);
    letter-spacing: 0.1em;
  }

  .project-title {
    font-family: 'Playfair Display', 'Noto Serif SC', serif;
    font-size: clamp(24px, 3vw, 40px);
    font-weight: 700;
    line-height: 1.15;
    margin-bottom: 8px;
    color: var(--white);
  }

  .project-subtitle {
    font-size: 14px;
    color: var(--gold);
    margin-bottom: 24px;
    font-weight: 300;
    letter-spacing: 0.04em;
  }

  .project-body {
    font-size: 16px;
    line-height: 1.85;
    color: var(--white-dim);
    font-weight: 300;
    margin-bottom: 32px;
  }

  .project-highlights {
    display: flex;
    flex-direction: column;
    gap: 14px;
    margin-bottom: 32px;
  }

  .highlight {
    display: flex;
    gap: 14px;
    align-items: flex-start;
    font-size: 14px;
    line-height: 1.7;
    color: var(--white-dim);
  }

  .highlight::before {
    content: '—';
    color: var(--gold);
    flex-shrink: 0;
    margin-top: 2px;
  }

  .highlight strong { color: var(--white); }

  .project-link {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    font-family: 'DM Mono', monospace;
    font-size: 12px;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: var(--gold);
    text-decoration: none;
    border-bottom: 1px solid rgba(201,168,76,0.3);
    padding-bottom: 2px;
    transition: all 0.2s;
  }

  .project-link:hover {
    color: var(--gold-light);
    border-color: var(--gold-light);
    gap: 12px;
  }

  /* EXPERIENCE */
  .experience { background: var(--bg2); }

  .exp-list {
    display: flex;
    flex-direction: column;
    gap: 2px;
  }

  .exp-item {
    background: var(--bg3);
    padding: 36px 40px;
    display: grid;
    grid-template-columns: 200px 1fr;
    gap: 40px;
    align-items: start;
    transition: background 0.3s;
  }

  .exp-item:hover { background: #1f2019; }

  .exp-company {
    font-size: 14px;
    font-weight: 700;
    color: var(--white);
    margin-bottom: 4px;
    letter-spacing: 0.03em;
  }

  .exp-period {
    font-family: 'DM Mono', monospace;
    font-size: 10px;
    color: var(--white-faint);
    letter-spacing: 0.1em;
  }

  .exp-role {
    font-size: 16px;
    font-weight: 600;
    color: var(--gold-light);
    margin-bottom: 10px;
  }

  .exp-desc {
    font-size: 14px;
    line-height: 1.8;
    color: var(--white-dim);
    font-weight: 300;
  }

  /* SKILLS */
  .skills { background: var(--bg); }

  .skills-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 2px;
  }

  .skill-group {
    background: var(--bg3);
    padding: 36px 32px;
  }

  .skill-group-title {
    font-family: 'DM Mono', monospace;
    font-size: 10px;
    letter-spacing: 0.2em;
    text-transform: uppercase;
    color: var(--gold);
    margin-bottom: 20px;
    font-weight: 500;
  }

  .skill-tags {
    display: flex;
    flex-wrap: wrap;
    gap: 8px;
  }

  .skill-tag {
    font-size: 12px;
    padding: 5px 12px;
    border-radius: 4px;
    background: var(--white-faint);
    color: var(--white-dim);
    letter-spacing: 0.04em;
    transition: all 0.2s;
  }

  .skill-tag:hover {
    background: var(--gold-dim);
    color: var(--gold-light);
  }

  /* CONTACT */
  .contact {
    background: var(--bg2);
    text-align: center;
    padding: 120px 7vw;
  }

  .contact-pre {
    font-family: 'DM Mono', monospace;
    font-size: 11px;
    letter-spacing: 0.2em;
    text-transform: uppercase;
    color: var(--gold);
    margin-bottom: 32px;
  }

  .contact-title {
    font-family: 'Playfair Display', serif;
    font-size: clamp(40px, 6vw, 80px);
    font-weight: 700;
    line-height: 1.05;
    margin-bottom: 48px;
  }

  .contact-title em {
    font-style: italic;
    color: var(--gold);
    font-weight: 400;
  }

  .contact-email {
    display: inline-block;
    font-family: 'DM Mono', monospace;
    font-size: clamp(14px, 2vw, 20px);
    color: var(--white);
    text-decoration: none;
    border-bottom: 1px solid var(--gold);
    padding-bottom: 4px;
    letter-spacing: 0.04em;
    transition: color 0.2s;
  }

  .contact-email:hover { color: var(--gold-light); }

  /* FOOTER */
  footer {
    background: var(--bg);
    border-top: 1px solid var(--border);
    padding: 32px 7vw;
    display: flex;
    justify-content: space-between;
    align-items: center;
  }

  .footer-left {
    font-family: 'DM Mono', monospace;
    font-size: 11px;
    color: var(--white-faint);
    letter-spacing: 0.08em;
  }

  .footer-right {
    display: flex;
    gap: 24px;
  }

  .footer-right a {
    font-family: 'DM Mono', monospace;
    font-size: 11px;
    color: var(--white-faint);
    text-decoration: none;
    letter-spacing: 0.08em;
    transition: color 0.2s;
  }

  .footer-right a:hover { color: var(--gold); }

  /* REVEAL */
  .reveal {
    opacity: 0;
    transform: translateY(28px);
    transition: opacity 0.7s ease, transform 0.7s ease;
  }
  .reveal.visible { opacity: 1; transform: translateY(0); }
  .reveal-delay-1 { transition-delay: 0.1s; }
  .reveal-delay-2 { transition-delay: 0.2s; }
  .reveal-delay-3 { transition-delay: 0.3s; }

  /* RESPONSIVE */
  @media (max-width: 900px) {
    .now-grid, .skills-grid { grid-template-columns: 1fr; }
    .project-item { grid-template-columns: 1fr; gap: 32px; }
    .project-meta { position: static; }
    .exp-item { grid-template-columns: 1fr; gap: 8px; }
    nav .nav-links { display: none; }
    body { cursor: auto; }
    .cursor, .cursor-ring { display: none; }
  }

  @media (max-width: 600px) {
    .skills-grid { grid-template-columns: 1fr 1fr; }
  }
</style>
</head>
<body>

<div class="cursor" id="cursor"></div>
<div class="cursor-ring" id="cursorRing"></div>

<!-- NAV -->
<nav>
  <div class="nav-name">Jackie <span>·</span> 查可欣</div>
  <ul class="nav-links">
    <li><a href="#now">近况</a></li>
    <li><a href="#projects">项目</a></li>
    <li><a href="#experience">经历</a></li>
    <li><a href="#skills">技能</a></li>
    <li><a href="#contact">联系我</a></li>
  </ul>
</nav>

<!-- HERO -->
<section class="hero">
  <div class="hero-bg"></div>
  <div class="hero-grid-overlay"></div>

  <div style="position:relative; z-index:1;">
    <div class="hero-eyebrow">哥伦比亚大学 SIPA · Class of 2027 · New York</div>
    <h1 class="hero-name">用数据与 AI <br><em>推动产品决策</em></h1>
    <p class="hero-desc">
      在气候能源与数据政策的交叉地带思考，<br>
      用<strong>产品思维</strong>发现问题，用<strong>代码</strong>把想法变成真实运作的系统。<br>
      从0到1独立搭建过服务 3,000+ 用户的平台。
    </p>
    <div class="hero-tags">
      <div class="hero-tag active">产品经理</div>
      <div class="hero-tag">数据分析</div>
      <div class="hero-tag">气候 · 能源</div>
      <div class="hero-tag">微信小程序</div>
      <div class="hero-tag">Columbia AI Club 联合主席</div>
    </div>
  </div>

  <div class="hero-scroll">Scroll</div>
</section>

<!-- NOW -->
<section class="now" id="now">
  <div class="section-label">现在</div>
  <div class="now-grid">
    <div class="now-card reveal">
      <div class="now-icon">📍</div>
      <div class="now-card-title">在哪里</div>
      <div class="now-card-body">
        <strong>哥伦比亚大学 SIPA</strong><br>
        气候/能源/环境方向<br>
        数据科学政策辅修<br>
        2025 年 5 月毕业
      </div>
    </div>
    <div class="now-card reveal reveal-delay-1">
      <div class="now-icon">🔍</div>
      <div class="now-card-title">在找什么</div>
      <div class="now-card-body">
        <strong>产品经理 / 数据分析师</strong><br>
        目标：中国互联网 / 科技行业<br>
        偏好有真实用户规模的产品岗<br>
        可接受 Data PM 方向
      </div>
    </div>
    <div class="now-card reveal reveal-delay-2">
      <div class="now-icon">🛠</div>
      <div class="now-card-title">在做什么</div>
      <div class="now-card-body">
        迭代<strong>志远出行</strong>——司机端留存与供给侧增长<br><br>
        担任 <strong>Columbia AI Club</strong> 联合主席<br><br>
        探索 AI × 能源政策的交叉领域
      </div>
    </div>
  </div>
</section>

<!-- PROJECTS -->
<section class="projects" id="projects">
  <div class="section-label">精选项目</div>

  <div class="project-item reveal">
    <div class="project-meta">
      <div class="project-num">01</div>
      <div class="project-tag gold">创业项目</div>
      <div class="project-period">2024 — 至今</div>
    </div>
    <div class="project-content">
      <h3 class="project-title">志远出行</h3>
      <div class="project-subtitle">纽约华人拼车 × 二手市场平台 · 微信小程序</div>
      <p class="project-body">
        发现纽约华人学生通勤痛点：同一路线 Uber 收费 $40，
        但路线高度重叠的乘客之间缺乏可信任的撮合工具。
        独立完成用户调研、产品设计、开发上线、冷启动全流程。
      </p>
      <div class="project-highlights">
        <div class="highlight"><strong>$8 vs $40</strong>——同路线价格低 80%，Fort Lee → 哥大</div>
        <div class="highlight"><strong>3,000+ 注册用户</strong>，已上线真实运营</div>
        <div class="highlight">洞察：拼车用户地址聚集 → 天然邻居 → <strong>有机扩展二手市场</strong></div>
        <div class="highlight">关键决策：用微信实名体系作为<strong>零成本信任基础设施</strong>，消解陌生人拼车顾虑</div>
        <div class="highlight">司机端摩擦优化：路线模板一键复用，显著提升供给侧复发率</div>
      </div>
      <a href="https://kz2595-coder.github.io/zhiyuan" class="project-link" target="_blank">
        查看产品展示页 →
      </a>
    </div>
  </div>

  <div class="project-item reveal">
    <div class="project-meta">
      <div class="project-num">02</div>
      <div class="project-tag green">企业项目</div>
      <div class="project-period">2023</div>
    </div>
    <div class="project-content">
      <h3 class="project-title">Vestas LCO 平台</h3>
      <div class="project-subtitle">内部项目管理工具 · PowerApps + SharePoint + Power BI</div>
      <p class="project-body">
        在全球风能领导企业 Vestas 独立端到端搭建生命周期优化平台，
        服务内部 PM 团队的研发项目管理与合规追踪需求。
      </p>
      <div class="project-highlights">
        <div class="highlight">服务 <strong>200+ 内部 PM 用户</strong>，覆盖 <strong>2,000+ 研发提案</strong></div>
        <div class="highlight">构建合规监控报告，追踪 OPEX/CAPEX 及 Stage 1–5 阶段合规状态</div>
        <div class="highlight">设计含多种计算模型的 ROI / 价值评估模块</div>
      </div>
    </div>
  </div>

  <div class="project-item reveal">
    <div class="project-meta">
      <div class="project-num">03</div>
      <div class="project-tag gold">数据分析</div>
      <div class="project-period">2023</div>
    </div>
    <div class="project-content">
      <h3 class="project-title">小红书风控策略分析</h3>
      <div class="project-subtitle">风险控制 · SQL 数据分析 · 策略提案</div>
      <p class="project-body">
        在小红书风控团队实习期间，独立完成金融 KOL 账号风险分析，
        提出的两项欺诈检测策略被采纳入官方文档。
      </p>
      <div class="project-highlights">
        <div class="highlight">SQL 分析 <strong>约 12 万条</strong>风控日志</div>
        <div class="highlight">独立提出并验证两项欺诈识别特征：企业微信联系信息 + 高频同质内容与外链跳转组合特征</div>
        <div class="highlight">策略覆盖大量金融 KOL 账号，<strong>被采纳入官方风控文档</strong></div>
      </div>
    </div>
  </div>
</section>

<!-- EXPERIENCE -->
<section class="experience" id="experience">
  <div class="section-label">实习经历</div>
  <div class="exp-list">
    <div class="exp-item reveal">
      <div>
        <div class="exp-company">小红书</div>
        <div class="exp-period">2023</div>
      </div>
      <div>
        <div class="exp-role">风险控制策略实习生</div>
        <div class="exp-desc">金融 KOL 账号欺诈检测，SQL 分析 12 万+ 风控日志，独立提案被采纳入官方文档</div>
      </div>
    </div>
    <div class="exp-item reveal reveal-delay-1">
      <div>
        <div class="exp-company">Vestas Wind Technology</div>
        <div class="exp-period">2023</div>
      </div>
      <div>
        <div class="exp-role">项目管理实习生</div>
        <div class="exp-desc">独立端到端搭建 LCO 平台，服务 200+ PM 用户与 2,000+ 研发提案，PowerApps + Power BI</div>
      </div>
    </div>
    <div class="exp-item reveal reveal-delay-2">
      <div>
        <div class="exp-company">UNIDO</div>
        <div class="exp-period">2022</div>
      </div>
      <div>
        <div class="exp-role">项目助理</div>
        <div class="exp-desc">联合国工业发展组织，参与可持续工业发展相关项目支持工作</div>
      </div>
    </div>
  </div>
</section>

<!-- SKILLS -->
<section class="skills" id="skills">
  <div class="section-label">技能</div>
  <div class="skills-grid">
    <div class="skill-group reveal">
      <div class="skill-group-title">产品</div>
      <div class="skill-tags">
        <span class="skill-tag">用户调研</span>
        <span class="skill-tag">产品设计</span>
        <span class="skill-tag">双边市场</span>
        <span class="skill-tag">增长策略</span>
        <span class="skill-tag">冷启动</span>
        <span class="skill-tag">Figma</span>
      </div>
    </div>
    <div class="skill-group reveal reveal-delay-1">
      <div class="skill-group-title">数据</div>
      <div class="skill-tags">
        <span class="skill-tag">SQL</span>
        <span class="skill-tag">Python</span>
        <span class="skill-tag">Power BI</span>
        <span class="skill-tag">统计建模</span>
        <span class="skill-tag">数据可视化</span>
      </div>
    </div>
    <div class="skill-group reveal reveal-delay-2">
      <div class="skill-group-title">开发 & 工具</div>
      <div class="skill-tags">
        <span class="skill-tag">微信小程序</span>
        <span class="skill-tag">PowerApps</span>
        <span class="skill-tag">SharePoint</span>
        <span class="skill-tag">Cursor</span>
        <span class="skill-tag">Claude</span>
        <span class="skill-tag">HTML/CSS</span>
      </div>
    </div>
    <div class="skill-group reveal reveal-delay-3">
      <div class="skill-group-title">语言</div>
      <div class="skill-tags">
        <span class="skill-tag">中文（母语）</span>
        <span class="skill-tag">英文（流利）</span>
      </div>
    </div>
  </div>
</section>

<!-- CONTACT -->
<section class="contact" id="contact">
  <div class="contact-pre">联系我</div>
  <h2 class="contact-title">在找<em>有意思的</em><br>机会和对话</h2>
  <a href="mailto:kz2595@columbia.edu" class="contact-email">kz2595@columbia.edu</a>
</section>

<!-- FOOTER -->
<footer>
  <div class="footer-left">© 2025 Jackie · 查可欣</div>
  <div class="footer-right">
    <a href="https://kz2595-coder.github.io/zhiyuan" target="_blank">志远出行展示页</a>
    <a href="https://github.com/kz2595-coder" target="_blank">GitHub</a>
  </div>
</footer>

<script>
  // Cursor
  const cursor = document.getElementById('cursor');
  const ring = document.getElementById('cursorRing');
  let mx = 0, my = 0, rx = 0, ry = 0;

  document.addEventListener('mousemove', e => {
    mx = e.clientX; my = e.clientY;
    cursor.style.left = mx + 'px';
    cursor.style.top = my + 'px';
  });

  function animateRing() {
    rx += (mx - rx) * 0.12;
    ry += (my - ry) * 0.12;
    ring.style.left = rx + 'px';
    ring.style.top = ry + 'px';
    requestAnimationFrame(animateRing);
  }
  animateRing();

  document.querySelectorAll('a, button, .now-card, .skill-tag, .hero-tag').forEach(el => {
    el.addEventListener('mouseenter', () => {
      cursor.style.transform = 'translate(-50%,-50%) scale(2)';
      ring.style.transform = 'translate(-50%,-50%) scale(1.5)';
      ring.style.borderColor = 'rgba(201,168,76,0.8)';
    });
    el.addEventListener('mouseleave', () => {
      cursor.style.transform = 'translate(-50%,-50%) scale(1)';
      ring.style.transform = 'translate(-50%,-50%) scale(1)';
      ring.style.borderColor = 'rgba(201,168,76,0.4)';
    });
  });

  // Scroll reveal
  const observer = new IntersectionObserver(entries => {
    entries.forEach(e => {
      if (e.isIntersecting) e.target.classList.add('visible');
    });
  }, { threshold: 0.12 });

  document.querySelectorAll('.reveal').forEach(el => observer.observe(el));
</script>

</body>
</html>
